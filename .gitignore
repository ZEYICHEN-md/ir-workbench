# 本机配置（本机配置与共享代码分离）
.ir-workbench/

# 三个旧项目：各自冻结在 GitHub 作为历史（ADR 0003），不嵌进本仓。
# 它们各自带 .git——不排除的话会被记成未初始化的子模块，内容根本不会被追踪。
0703_Travel_Pulse/
database_matain/
peers_rs_update/

# 一次性产物
scratch/
_tmp/

# 每期原件（券商周报、研报 PDF、STR 表等）：只服务当期，体积大且多为第三方材料。
# 不进 git —— 中金周报单份 6.5 MB，每周一份一年就是几百 MB。
# 可核对性由 manifest 里记录的文件名与 SHA-256 保证，不靠存原件。
inputs/**
!inputs/**/.gitkeep

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Excel 临时锁文件
~$*.xls*

# 环境
.venv/
.env
