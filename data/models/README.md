# Peers 权威 Model

三份 Excel **不进 Git**（体积大，且是活的底稿）。分发 zip / clone 之后，由交付人把文件拷到本文件夹，再对 Agent 说「锁定这三份 Model」。

当前命名约定（以实际文件名为准，Agent 按通配符找候选）：

| 用途 | 文件名大致长这样 |
|---|---|
| BKNG / EXPE / ABNB 共用 | `peers data comparison_*.xlsx` |
| 美团 | `Meituan*.xlsx` |
| 同程 | `Tongcheng*Model*.xlsx` |

不要手改格子来「更新一季」。更新只出副本，副本在 `outputs/peers-model/`。
