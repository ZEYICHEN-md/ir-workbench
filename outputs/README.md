# 成品

Agent 做完的东西在这里，按业务分子文件夹。不要手改生成的格子。

| 随包走 | 只留本机 |
|---|---|
| `news-digest/` 里的 Markdown（精选真源） | 同目录 HTML / PDF（可用 Markdown 再导出） |
|  | `peers-model/` 更新副本 |
|  | `shareholder-list/` 生成的 xlsx |
|  | 卖方研报抽取页、专家访谈 TXT |

要取回时问 Agent：「这期新闻精选在哪」「BKNG 模型副本在哪」「股东名册在哪」。
说明见 [docs/MAP.md](../docs/MAP.md)。
