# ADR 0012：上期 SH 用工作簿内快照，不用桌面外链

- 日期：2026-09-03（源包 0002；工作台编号见 [0010](0010-shareholder-list-module.md)）
- 状态：已采纳

旧 SH Summary 用 `[1]SH Summary` 指向另一本本地 xlsx，换路径即断。生成器把上期排名、股数、占比写入 `SH Prior`（默认隐藏，交表时不出现在标签栏），本期公式只引用本工作簿。
