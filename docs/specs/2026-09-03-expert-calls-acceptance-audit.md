# expert-calls 真实批次验收审计

- 日期：2026-09-03
- 批次：`20260901-190000`
- 结论：`validated`

## 验收结果

- 7 份真实 PDF 已抽取；`intel-draft.json` 共 34 条。
- A 类批准稿 11 条，全部逐 ID 存在于 `data/intel/entries.jsonl`。
- B 类 14 条，全部逐 ID 存在于 `data/intel/deferred.jsonl`。
- 剔除 9 条，批准稿保留完整原编号清单。
- 三组互斥且 `11 + 14 + 9 = 34`，没有未处置条目。
- 飞书分支已发布 3 条并回读；manifest 记录 3 个 block id。
- 公司档案投影与正式库通过 doctor 检查。

## 漏写发现与修复

首次审计发现，`intel-review-approved.md` 把 14 条列为暂缓，但批准 deferred 文件及待核池
最初只有原编号 3、4、9、10、15、17、20、27。原编号 5、6、11、18、19、25 共 6 条既不在
正式库，也不在待核池，且未列入剔除清单。

2026-09-03 用户在 Agent 明确说明上述 6 条均为 B 类、将写入待核池后回复“OK，那就修补这个漏写问题”。
Agent 随即使用正式 `intel defer` 流程预演：新增 6、已存在 8、拒绝 0、冲突 0；确认结果无误后
提交写入。回读得到 A=11、B=14、剔除=9、合计=34，A/B 无重叠、未处置清单为空。

## 后续契约

后续批次必须满足 `A + B + 剔除 = 草稿总数`，三组互斥且逐条保留稳定 ID/原编号；A 类逐 ID
对上正式库，B 类逐 ID 对上待核池，剔除项在批准产物中保留原因。飞书 run 完成数不能替代情报
分流验收。