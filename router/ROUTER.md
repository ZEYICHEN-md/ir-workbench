# 顶层路由 —— 人的唯一入口

> Agent 读这份文件决定「用户想干什么、该走哪个模块」。
> 业务接手人只说自然语言，不需要读这里，也不需要知道任何命令。

## 工作原则

1. **先归类，再动手。**接到请求先落到下表某一行；落不进去就问，别猜。
2. **CLI 是你的手，不是用户的。**不要把命令贴给用户让他自己跑；你代跑，然后用人话汇报。
3. **门禁不可绕过。**标了「须明确确认」的动作，没听到用户明确措辞前停在 dry-run / 草稿，并说明「需要你说什么才能继续」。
4. **不代选、不代判。**工作簿多候选让用户指定；数据冲突列出数值、口径、来源、日期后等确认。
5. **不确定性要显式返回。**用 `success` / `partial` / `blocked` / `failed` 四态汇报，说明缺什么、下一步是什么。

## 意图路由表

| 用户可能这么说 | 域 | 你要做的 | 门禁 |
|---|---|---|---|
| 做这周新闻精选 / 更新新闻周报 / 行业新闻精选 | `news-digest` | `plan` → `recall`（枚举 + 我另跑 5 条补充检索）→ `check` 查重 → **人写稿** → `validate` → `export` → `log` 登台账 → 沉淀情报库 → **更新飞书 Wiki 云文档**（旅业资讯库 + 历史汇总） | 云文档写入前后自动回读复核；卡片周报不在此步自动触发 |
| 做这周飞书卡片周报 / 发飞书卡片周报 / 发周报卡片 | `news-digest` | 套 `modules/news_digest/references/feishu-im-card.md`：读取当期已定稿精选 + `travel.json` 最新一周 KPI + 已确认洞察；机器人私聊发给用户，由用户转发 | 缺精选定稿或最新一周三序列则 `blocked` 并说明缺什么 |
| 这周有什么新闻可以写 / 有哪些候选 | `news-digest` | `recall` 枚举 + 补充检索对账，并跨期查重后给候选清单 | — |
| 这条是不是上期写过了 | `news-digest` | `check`（台账只报疑似，判断交给人） | — |
| 更新国内行业数据 / 补数 / 更新看板 / 同步指标 | `industry-data` | 读底稿 → merge 逐格 diff → **自动识别周/月/季变动并只出对应洞察中文草稿** → 给用户审查 → 用户说「写入上线」后入库、英译、看板、发布 | 清空格超阈值须确认；**不再问要不要刷新洞察，只在最后问是否写入上线** |
| 这是最新的中金周报（附表格）/ 用这份中金表更新看板 | `industry-data` | `str-apply`（dry-run 摆出要填哪几格）→ **确认后写底稿** → merge → 按变动粒度出洞察草稿 → 审查后写入上线 | 写底稿须明确确认；**只填空格，人手填过的值绝不覆盖** |
| 同步飞书（多维表） | `industry-data` | **非默认、半弃用**；用户明确要求时才出待写入清单 + 冲突清单 | **须明确说「写入」** |
| 更新洞察 / 刷新洞察 | `industry-data` | 默认按最近一次 merge 的实际变动粒度出中文草稿；明确说全量才周/月/季全写 | **须用户确认中文后**才入库，再译英文 |
| 更新 X 月航空数据 / 民航局月报出了 | `aviation-monthly` | 抓官方 → 校验 → dry-run | **须明确确认**才写指标底稿 |
| 港美成交占比 / 南向持股 / 港股行情 | `hk-market` | 查询并报数（成交占比只看最近完整财年 FY） | — |
| XX 公司这季度做了什么 / 竞对研究 | `competitor-intel` | 按公司检索档案 | — |
| peers 在 XX 主题上都做了什么 / 都怎么说 | `competitor-intel` | 按主题横切检索 | — |
| 沉淀这期新闻 / 回填历史情报 | `competitor-intel` | `deposit` 出打标草稿 → 人核对 → `--commit` 入库并重建档案 | 主题是关键词猜的，**须核对后**才入库 |
| 这条访谈 / 电话会口径也存一下 | `competitor-intel` | 已有结构化原话和位置时直接 `intel add`；若输入是整份专家访谈 PDF，改走下一行的 `expert-calls` | 正式入库须人工核对并明确确认 |
| 我发几篇专家访谈 / 读出有价值信息放进公司情报库 | `expert-calls` | PDF 按页抽取 → 识别公开渠道难以获得的行业事实、公司信息、内部经营数据与机制 → 为所有访谈生成带原话和页码的内部情报草稿 → `intel add` 预演 | **情报草稿与飞书选择独立；正式入库须人工核对并明确确认** |
| BKNG 出财报了 / 做业绩总结 / 更新 Appendix | `peers-appendix` | 材料入库 → 取数建表贴图 → 门禁 → 用户写正文 | 门禁 FAIL 硬停；战略段由人写 |
| 看看这些专家访谈哪些值得写 / 做 Expert Call 候选 | `expert-calls` | 先完成公司情报提取，再对飞书展示价值做六维透明评分并生成 A/B/C 候选排序；大型跨国平台相关业务高管优先，亚太重点市场的区域专家可凭高相关性保留到 B 档 | **情报沉淀不受飞书 include 影响；排序只辅助，由人决定哪些进入草稿** |
| 只提取专家访谈摘要 / 先出 Expert Call 草稿 | `expert-calls` | 同次阅读先出情报草稿，再出候选排序 → 人选择 → 校验溯源 → 只为选中项渲染 revision 1680 callout 草稿；不写飞书 | 情报入库与飞书发布分别确认 |
| 更新 Expert Call / 把专家访谈写入飞书 | `expert-calls` | 完成情报草稿、候选排序、人工选择、callout 草稿与精确判重；用户审阅后逐条写入并回读 | **发布到飞书须明确说「发布专家访谈精选」；飞书发布不触发情报入库** |
| 帮我摘这份研报 | `sellside-research` | 读 PDF 出摘要 | — |
| 能做什么 / 现在什么状态 / 跑不跑得起来 | Control Plane | `ir doctor` + `ir status` | — |
| 换一份新的国内行业数据 | Control Plane | 列候选 → **用户指定** → `ir config set` | 绝不代选 |
| 更新工作台 | Control Plane | 拉取新版本并复检 | — |

## 落不进表里怎么办

说清「这件事目前不在工作台范围内」，并给出最接近的能力。**不要临场发挥造一条新流程**——新增能力要走决策并写进这张表，否则半年后会出现第二套入口。

## 明确不做的事

- 五部分旅行行业周报：已停用（见 `docs/GLOSSARY.md`）。用户要行业数据指向看板，要港股走内部查询。
  `ir news validate` 会主动拦含 `## 三、` 及以后的稿子——不拦的话有人照旧模板写，工具默默接受就发出去了。
  内部群周报卡片只有「新闻 + 行业数据」两节，不是五部分复活，也不含卖方和港股。
- **代写新闻精选的正文。**召回、查重、校验、导出、沉淀都自动；那两三句 so-what 是判断，人写。
- 新闻精选里写任何携程自己的动作与表述。判定标准是「携程是不是这条新闻的当事方」；
  凡携程当事方的境内事件 IR 都已第一时间掌握，写进去是浪费篇幅。
- 未经用户明确要求的对外发布。
- 替用户决定数据口径。

## 相关

- 功能清单（给同事看）：`docs/CAPABILITIES.md`
- 统一语言：`docs/GLOSSARY.md`
- 决策记录：`docs/adr/`
- 各域操作流程：`modules/<域>/SKILL.md`
